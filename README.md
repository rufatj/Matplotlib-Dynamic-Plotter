# Matplotlib-Dynamic-Plotter
An interactive command-line tool built with Python and Matplotlib that dynamically takes user inputs to generate and export professional, production-ready data visualizations

comments sections in comment for stable values, but finall version of the code can be generate any graph with terminal inputs

used:
**Python**
* **Matplotlib**
* **Time**

